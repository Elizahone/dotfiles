# -*- mode: snippet -*-
# name: cf
# key: <cf
# --

#include <bits/stdc++.h>
using std::cin, std::cout, std::string;

void solve() {
    ${0}
}

int main() {
    std::ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    ${1}solve();
    return 0;
}
