声音控制
pavucontrol

亮度管理
light(aur)

GTK 设置

nwg-look

护眼模式

gammastep


输入法配置
fcitx5 fcitx5-configtool fcitx5-{qt,gtk}

notification
mako and libnotify(notify-send)


nvidia 驱动 btw fuck nvidia
使用 envycontrol 生成大部分配置文件
在 /etc/modprobe.d/nvidia.conf 添加 `options nvidia.ko NVreg_PreserveVideoMemoryAllocations=1`然后可以正常使用suspend


视频查看  mpv
图片查看  imv

截图录屏
obs-studio
grim  slurp
